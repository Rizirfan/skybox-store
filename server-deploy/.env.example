# Copy to .env and fill values
# Option 1: Set DATABASE_URL directly
# DATABASE_URL=postgresql://user:password@localhost:5432/drive_db

# Option 2: Set DB parts and the server will build the connection string
DB_USER=postgres
DB_PASSWORD=irfan
DB_HOST=localhost
# If your db name contains spaces, wrap it in quotes when using it in CLI; connection string builds safely with encodeURIComponent
DB_NAME=clone drive
DB_PORT=5433

JWT_SECRET=change-this-to-a-random-secret
PORT=4000
UPLOAD_DIR=./uploads
